# 发布就绪检查

## 结论

本地变更满足交付条件，可以结束本次实现流程；本次不执行 commit、push 或发布。

## 完成项

- 新运行默认使用用户级单文件 `run.json`，项目内不生成流程控制 JSON。
- 旧 v1/v2 项目运行继续使用旧后端，不迁移、不删除。
- 新后端 FULL 17 阶段流程、确认、返工、健康、Git、严格复核和收口集成测试通过。
- 独立代码审查最终为 PASS，发现的问题均有回归测试。
- 全量测试 143/143、存储专项 26/26、package、语法编译和差异检查通过。
- README、中文 README、skill 入口、workflow 和维护文档已同步。
- Git 变更检查通过，变更均保留在当前工作区，未提交。

## 兼容与回滚

- 需要新建旧格式运行时，可设置 `LOOPX_STATE_BACKEND=project`。
- `LOOPX_STATE_DIR` 可覆盖用户状态根，适用于 CI、测试和受控工作区。
- 已存在的新格式运行始终读取原容器；回滚开关不会迁移或删除它。

## 未覆盖与发布提示

- 项目标识绑定项目真实路径，项目移动或重命名后不会自动找回旧运行。
- Windows、网络文件系统、远端 CI 和真实多进程压力测试未执行。
- 未执行版本更新、commit、push、真实发布或外部系统调用。
