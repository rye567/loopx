{
  "type": "object",
  "required": [
    "required",
    "mode",
    "status",
    "artifact",
    "unanswered_questions",
    "can_skip"
  ],
  "properties": {
    "required": {
      "type": "boolean"
    },
    "mode": {
      "type": "string",
      "enum": ["LIGHT", "STANDARD", "FULL"]
    },
    "status": {
      "type": "string",
      "enum": ["NOT_STARTED", "IN_PROGRESS", "PASS", "CHANGES_REQUIRED", "BLOCKED", "NEED_HUMAN"]
    },
    "artifact": {
      "type": "string"
    },
    "unanswered_questions": {
      "type": "integer"
    },
    "can_skip": {
      "type": "boolean"
    },
    "blocking_questions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "confirmed_facts": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "assumptions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
